# Android 构建指南 - 同行 v0.1.8

## 📱 应用信息
- **应用名称**: 同行
- **包名**: com.tongxingapp
- **版本号**: 0.1.8
- **Version Code**: 8
- **构建类型**: Release

## 🔑 签名信息
已成功生成签名密钥文件：
- **密钥文件**: `android/app/tongxing-release.keystore`
- **别名**: tongxing
- **密码**: tongxing123
- **密钥密码**: tongxing123
- **有效期**: 10000天
- **算法**: RSA 2048位
- **颁发者**: CN=同行, OU=移动开发, O=同行科技, L=上海, ST=上海, C=CN

## 📦 生成的APK文件
已成功构建发布版本APK：
- **文件位置**: `android/app/build/outputs/apk/release/app-release.apk`
- **文件大小**: ~1.92GB
- **构建时间**: 2026-07-11

## 🛠️ 构建命令

### 清理项目
```bash
cd android
./gradlew clean
```

### 构建发布版本
```bash
cd android
./gradlew assembleRelease
```

### 构建调试版本
```bash
cd android
./gradlew assembleDebug
```

### 安装到设备
```bash
cd android
./gradlew installRelease
```

## 🆕 新功能 v0.1.8

### 1. AI消息编辑功能
- 用户可以长按AI生成的消息进行编辑
- 编辑后的消息会立即更新并保存到数据库
- 支持修改AI生成的所有消息内容

### 2. 导出/导入优化
- 改进了头像字段的导出/导入逻辑
- 确保用户自定义头像在导出/导入过程中不被丢失
- 支持导入时修改角色信息

### 3. 记忆系统改进
- 调整记忆触发阈值为75%
- 优化对话总结逻辑，最多处理最近20条消息
- 改进了记忆提取的精确度

### 4. 构建工具增强
- 新增专用构建脚本 `构建APK_v0.1.8.bat`
- 改进的错误处理和用户提示
- 更详细的构建进度信息

### 5. 性能优化
- 优化的Token计数算法
- 改进的上下文管理
- 减少不必要的重新渲染

## 📱 安装说明

### 方法1: 直接安装APK
1. 将 `app/build/outputs/apk/release/app-release.apk` 复制到Android设备
2. 在设备上找到并点击APK文件进行安装
3. 如果提示"禁止安装未知来源应用"，请先允许安装未知来源应用

### 方法2: 通过ADB安装
```bash
# 连接设备后
cd android
adb install app/build/outputs/apk/release/app-release.apk
```

## 🔧 构建配置详情

### build.gradle 配置
```gradle
android {
    defaultConfig {
        applicationId "com.tongxingapp"
        versionCode 5
        versionName "0.1.5"
        minSdkVersion 24
        targetSdkVersion 36
        compileSdkVersion 36
    }
    
    signingConfigs {
        release {
            storeFile file('tongxing-release.keystore')
            storePassword 'tongxing123'
            keyAlias 'tongxing'
            keyPassword 'tongxing123'
        }
    }
    
    buildTypes {
        release {
            signingConfig signingConfigs.release
            minifyEnabled false
            proguardFiles getDefaultProguardFile("proguard-android.txt"), "proguard-rules.pro"
        }
    }
}
```

### AndroidManifest.xml 权限
```xml
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
<uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
```

## 📝 注意事项

### 1. 签名安全
- 签名密钥文件已生成并存储在项目中
- 请妥善保管签名密钥，这是发布应用的重要凭证
- 建议将签名信息存储在安全的地方

### 2. 应用功能
应用包含以下主要功能：
- ✅ AI聊天对话功能
- ✅ 消息重新生成功能
- ✅ 用户消息编辑功能
- ✅ 本地模型支持
- ✅ 数据库存储

### 3. 兼容性
- **最低Android版本**: 7.0 (API 24)
- **目标Android版本**: Android 14 (API 34)
- **编译SDK版本**: Android 14 (API 36)

### 4. 后续构建
如需重新构建，可以直接运行：
```bash
cd android && ./gradlew assembleRelease
```

## 🚀 发布到应用商店

### 准备材料
1. **应用截图**: 准备5张应用截图（手机尺寸）
2. **应用图标**: 准备512×512像素的应用图标
3. **应用描述**: 撰写应用功能介绍和使用说明
4. **隐私政策**: 如果需要，准备隐私政策链接

### 发布流程
1. 注册开发者账号（Google Play Console）
2. 创建新应用
3. 上传APK文件
4. 填写应用信息
5. 设置价格和分发范围
6. 提交审核

## 🔒 签名密钥备份

**重要**: 请务必备份签名密钥文件 `tongxing-release.keystore` 和密码！

如果丢失签名密钥，将无法：
- 更新现有应用
- 发布新版本到同一包名
- 使用相同的签名证书

建议的备份方式：
1. 将密钥文件加密后上传到云存储
2. 打印密钥信息并物理保存
3. 使用密码管理器保存密码

---

**构建完成时间**: 2026-07-08 08:49
**构建状态**: ✅ 成功
**APK文件**: `app-release.apk` (可分发版本)